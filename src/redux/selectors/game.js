export const getField = (state) => state.game.field;

export const getGameState = (state) => state.game.gameState;

export const getTimerActive = (state) => state.game.timerActive;

export const getCurrentMinesCount = (state) => state.game.currentMinesCount;

export const getSmileState = (state) => state.game.smileState;
